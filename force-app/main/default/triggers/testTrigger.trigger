trigger testTrigger on Case (before insert, after insert, before update, after update, before delete, after delete) {
    // if (trigger.isBefore) {
    //     system.debug('isBefore'+trigger.isBefore); 
    //     if (trigger.isInsert) {
    //         system.debug('Before insert Trigger'+trigger.isInsert);
            
    //     }
    //     if (trigger.isUpdate) { 
    //         system.debug('Before update trigger'+ trigger.isUpdate);
            
    //     }
    //     if (trigger.isDelete) {
    //         system.debug('Before delete trigger'+ trigger.isDelete);


            
    //     }
    // }
    // if (trigger.isAfter){
    //     system.debug('isAfter'+trigger.isAfter); 

    // }
    // if (trigger.isInsert){
    //     system.debug('isInsert'+trigger.isInsert); 

    // }
    // if (trigger.isUpdate){
    //     system.debug('After update'+trigger.isUpdate); 

    // }
    // if (trigger.isDelete){
    //     system.debug('After delet'+trigger.isDelete); 

    // }
}