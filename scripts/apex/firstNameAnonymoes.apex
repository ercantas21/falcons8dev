FirstLesson burak = new FirstLesson();
burak.ibnHacer();


integer num= 12;
if(math.mod(num,2)==0){
    system.debug('cift');

}else{
    system.debug('tek');
}

integer num =12;
if(math.mod(num,2)==0){
    system.debug(cift)
}


date newDate = data.newInstance(1981.07,25);
date newDate = date.newInstance(1980,06,06);